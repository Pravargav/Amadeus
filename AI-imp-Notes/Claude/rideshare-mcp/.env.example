# Copy to .env and fill in.
CLAUDE_MODEL="claude-sonnet-5"
ANTHROPIC_API_KEY=""

# Sampling approval policy: "ask" (human-in-the-loop) or "auto" (trust the server).
# "ask" is what a real client should do -- the server is spending YOUR tokens.
SAMPLING_APPROVAL="ask"
