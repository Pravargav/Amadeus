### `/schedule`

Simply the entry point for creating a **Routine** without leaving your terminal.

You run `/schedule` inside Claude Code and describe what you want in plain language — it creates the same underlying Routine, just conversationally instead of through a separate config screen.

**Same idea, either entry point** — pick whichever fits your flow.
